[pytest]
markers =
    e2e: end-to-end UI tests (Selenium)
    api: API-level tests (requests)
    nondestructive: safe to run against the target URL (required by pytest-selenium's
        sensitive-url protection, which otherwise skips anything it deems destructive
        against domains like *.vercel.app)
testpaths = tests
